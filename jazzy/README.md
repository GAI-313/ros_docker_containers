# jazzy
## build
- CPU<br>
    ```bash
    docker compose build jazzy-cpu
    ```

## container bringup
detach
- CPU<br>
    ```bash
    docker compose up -d jazzy-cpu
    ```

## container entry
detach
- CPU<br>
    ```bash
    docker compose exec jazzy-cpu bash
    ```